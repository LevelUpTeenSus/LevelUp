specVersion: "v1"
serviceId: "levelupteensus-service"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "levelupteensus-database"
      cloudSql:
        instanceId: "levelupteensus-instance"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
